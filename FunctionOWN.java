public class FunctionOWN
{
    public static void main()
    {
        double a=4,b=5,c;
        c=(Math.sqrt(Math.pow(a+b,2))+(Math.pow(a,2))+(Math.pow(b,2)))+(Math.sqrt((Math.pow(a,2)+Math.pow(b,2))));
        System.out.println("Values : "+c);
    }
}
